# 从真实时刻找到 Agent：生活、工作、交流的收敛框架

这份文件替代“先列产品方向”的做法。暂不决定题目，也不继续使用此前的通用 Slack、handoff、exception closer 方向。先找出一个普通 chatbot 或固定自动化无法自然完成的真实时刻，再决定是否值得做 Agent。

## 一句话定义

Agent 不是“会聊天的模型”，而是一个能围绕目标持续 **观察环境 → 判断下一步 → 调用工具行动 → 检查结果 → 必要时调整或请人介入** 的系统。

只有“回答/生成”通常是模型能力；出现跨步骤的自主选择、真实行动和反馈闭环，才开始需要 Agent。

## 什么时候真的需要 Agent

一个场景越多满足以下条件，越适合 Agent：

1. **目标清楚，但路径每次不同。** 不能提前写死所有步骤。
2. **必须读取正在变化的环境。** 屏幕、声音、现场、设备、多人状态或外部系统会影响下一步。
3. **需要行动，不只是回答。** 要操作软件、协调人、控制设备、创建或修改真实对象。
4. **行动后必须观察结果。** 成功、失败、歧义会决定下一步，而不是“一次调用结束”。
5. **中途经常出现例外。** Agent 能根据情境改变计划。
6. **结果可以验证或撤销。** 能看到 receipt、状态变化、传感器反馈或人工确认。
7. **人在某个节点确实来不及、看不到或腾不出手。** 代理性补的是人的注意力、动作或持续性缺口。

一个简单判断式：

> Agent 价值 = 动态环境 × 多步行动 × 反馈适应 × 人的注意力缺口

任一项接近零，都应该先考虑更简单的产品形式。

## 什么时候不要用 Agent

| 情况 | 更合适的方案 |
|---|---|
| 步骤稳定、规则可以完整写出 | 普通代码、workflow automation |
| 只需查一条资料或生成一次内容 | 搜索、RAG、单次模型 API |
| 用户必须逐步做所有决定 | Copilot / 表单 /向导式 UI |
| 动作不可逆且无法可靠核验 | 不应自治；必须人工批准 |
| 极低延迟、极高确定性的控制 | 传统软件或专用模型 |
| 没有工具、状态或外部行动 | 这是 chatbot，不是 agent |

## 五级产品判断

1. **普通软件**：规则固定，输入 A 永远执行 B。
2. **AI 功能**：分类、抽取、生成或搜索一次。
3. **Assistant**：给建议或草稿，用户自己执行。
4. **Copilot**：持续理解上下文，但用户逐步驾驶。
5. **Agent**：用户给目标，系统自行选择若干步骤和工具，并根据结果继续。

Hackathon 不需要追求第五级的最大自治。一个窄场景中完整可靠的 4→5 级闭环，通常比“什么都能做”的通用 Agent 更可信。

## 不从行业找，先从“时刻”找

今晚先回忆你自己最近两周的生活、工作和交流，记录 20 个具体瞬间。句式必须是：

> 当 ___ 正在发生时，我/某类人因为 ___，不得不反复 ___，但真正想要的结果是 ___。

重点观察这些缝隙，但它们只是发现问题的镜头，不是产品方向：

- 双手或双眼被占用的时刻
- 被打断、恢复上下文的时刻
- 等待一个不确定结果的时刻
- 多个人互相猜测进度或意图的时刻
- 身体、房间、设备正在改变而软件不知道的时刻
- 某人无法替自己表达、行动或持续关注的时刻
- 专家需要同时看环境、流程和异常的时刻
- 一件小事必须跨多个系统才能真正完成的时刻

不要写“医疗 Agent”“旅行 Agent”。要写成“凌晨急诊时，家属一边陪病人一边反复向保险方确认授权，无法同时整理每次电话的证据”。越像一幕电影，越接近可做的 niche。

## 六道淘汰测试

每个时刻依次问：

1. **Chatbox loss test**：搬进聊天框后会失去什么关键 context 或行动能力？答不出来，淘汰。
2. **Agency test**：系统是否真的需要自己选择下一步？若只执行固定三步，用 workflow。
3. **Action test**：它完成了什么可观察的现实变化？若只有回答，淘汰。
4. **Feedback test**：它怎样知道刚才做对了？没有验证信号就降级为建议工具。
5. **Human-boundary test**：什么时候必须停下来找人？说不清边界，风险太高。
6. **Niche-truth test**：能否指出一个非常具体的人、地点和十分钟内发生的任务？若仍是“所有人提高效率”，继续缩小。

## 从 20 个时刻收敛到 1 个

每项 0–2 分，总分 16：

| 维度 | 0 分 | 2 分 |
|---|---|---|
| 非聊天框必要性 | 聊天完全一样 | 环境/设备不可替代 |
| 代理必要性 | 固定流程 | 必须动态选步骤 |
| 真实行动 | 只输出文字 | 改变外部状态 |
| 可验证性 | 无法知道对错 | 有即时反馈/凭证 |
| 用户具体度 | 所有人 | 一个清晰角色 |
| 时刻具体度 | 长期模糊问题 | 十分钟内的一幕 |
| 演示性 | 只能讲 | 两分钟能看懂变化 |
| 可做性 | 依赖大量集成 | 明天能做完整闭环 |

先保留最高的 3 个，再给每个写：

- **Before**：人现在狼狈地怎么做。
- **Trigger**：Agent 如何知道该出现。
- **Loop**：它观察、行动、验证的三步。
- **Human control**：哪一步由人确认。
- **After**：屏幕、现场或系统发生了什么可见变化。

只有写完这五行仍然让人觉得“哦，这个时刻确实需要它”，才进入技术选型。

## 从获奖作品反推的真正规律

- **Toy Story 6** 的强处不是 edge LLM，而是“白血病儿童在治疗环境里需要持续陪伴和监测”这个极窄的人与场所。
- **AI Comrade** 的强处不是 AR，而是实验者双手、视线和注意力都在实验上，系统必须在现场发现步骤错误。
- **Ensure** 的强处不是 voice，而是医疗紧急时人无法同时照顾病人、打保险电话、争取授权并保留证据。
- **Bones AI** 的强处不是通用助手，而是它进入当前 macOS 窗口、看见正在做的事，并就地生成可操作界面。
- **Agent-Mon** 的技术并非最复杂；它把多 Agent 编排翻译成任何人都懂的游戏空间、角色移动和 A/B 决策。

共同模式不是 Voice、AR 或多 Agent，而是：**一个被忽略的具体时刻 + 环境给予的新信息/动作 + 一眼可见的闭环 + 恰到好处的人类控制。**

## 明天选题前的停止条件

在满足下面四句前，不写核心代码：

1. “这是给 ___ 在 ___ 时用的。”
2. “普通聊天框做不到，因为它看不到/做不了 ___。”
3. “Agent 会自行完成 ___，再用 ___ 验证。”
4. “演示中观众会亲眼看到 ___ 从 A 变成 B。”

## 口述 brainstorm 记录格式

你可以继续边看边说。我会从每段口述中只抽取四类信号：

- **被你否掉的模式**：不再换皮重复。
- **你被打动的具体瞬间**：优先保留。
- **你自己的真实经历**：作为 niche 证据。
- **可演示的环境或动作**：后续用于收敛。

现在不急着给出新一轮“七八个方向”。下一步应先积累 20 个真实时刻，再用上面的六道测试一起砍到 3 个。
