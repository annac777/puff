# Provider is explicit. Keep credentials server-side; never put them in the extension.
OFFRAMP_PROVIDER=openrouter
OFFRAMP_MODEL=openai/gpt-5.6-sol
OPENROUTER_API_KEY=
EXA_API_KEY=
# The dev server also reads the existing ignored starter-kit/.env without copying keys.
