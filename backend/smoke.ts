// Explicit live test: synthetic context only, never the user's design.
import './server.js';
import {interpret,research,type Context,type Trace} from './agent.js';
const context:Context={browser:null,figma:{sessionId:'synthetic-smoke',fileName:'SYNTHETIC TEST',fileUrl:'',page:{id:'1:1',name:'Checkout'},selection:[{id:'2:1',name:'Mobile Checkout',type:'FRAME',pageId:'1:1'}],frames:[{id:'2:1',name:'Mobile Checkout',type:'FRAME',pageId:'1:1'},{id:'2:2',name:'Tablet Checkout',type:'FRAME',pageId:'1:1'}],changes:[{id:'2:1',name:'Mobile Checkout',type:'PROPERTY_CHANGE',properties:['itemSpacing'],origin:'LOCAL',at:Date.now()}],lastChangeAt:Date.now(),capturedAt:Date.now()}};
async function main(){const trace:Trace[]=[];const draft=await interpret(context,'Compare the mobile checkout with the tablet frame when I return.',trace,AbortSignal.timeout(90_000));console.log(JSON.stringify({test:'live checkpoint',component:draft.component,anchors:draft.anchorIds,trace,nextStep:draft.nextStep}));const workTrace:Trace[]=[];const result=await research('Find two official accessibility references for touch target sizes in mobile interfaces.',workTrace,AbortSignal.timeout(120_000));console.log(JSON.stringify({test:'live research',trace:workTrace,sources:result.sources.map(s=>s.url),summary:result.summary}));}
main().catch((error)=>{console.error('Live test failed:',String(error?.message||error).replace(/(?:sk-|key-)[A-Za-z0-9_-]+/g,'[redacted]').slice(0,450));process.exitCode=1;});
